
# Backend API Configuration
EXPO_PUBLIC_BACKEND_URL=https://your-backend-url.com

# Expo Push Notifications
# No additional configuration needed - uses Expo's push notification service
# Push tokens are automatically generated when notifications are initialized
